# Core Dependencies
google-genai>=1.0.0
pypdf>=6.0.0
python-dotenv>=1.1.0
aiohttp>=3.9.0

# RAG Pipeline - MINIMAL (runtime only needs numpy)
numpy>=1.24.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Development/Indexing ONLY (uncomment when running index_knowledge.py)
# sentence-transformers>=2.2.0  # For generating embeddings - NOT needed at runtime
# tiktoken>=0.5.0  # For token counting during indexing - NOT needed at runtime
